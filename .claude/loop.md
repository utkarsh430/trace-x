If the previous TRACE-X work was interrupted by a Claude usage limit, resume the
currently authorized phase from where it stopped.

Before continuing, inspect `docs/PROGRESS.md`, `git status`, and the relevant
phase in `docs/ROADMAP.md` so completed work is not repeated.

Continue only the current phase. Do not start the next phase without explicit
approval.

If the current phase is already complete, stop and wait.