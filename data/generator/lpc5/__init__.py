"""`LPC-5`: the eval-v2 label-proxy acceptance criterion (eval/track_a/criteria/lpc-5.md).

Evaluation-side only: labels come from the generator's in-memory rows, never from PostgreSQL. No
module under `packages/`, `services/` or `mcp_servers/` may import this package.
"""
