"""Shared conformance suites.

One suite per port, run against every adapter of that port. `SourceAdapter` x2
(Phase 1 and Phase 4B), `GraphStore` x3, `LLMProvider` x4, `ActionExecutor` x2
(docs/TESTING.md §3).
"""
